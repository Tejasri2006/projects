<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All users</title>
    <style>
      table,
      th,
      td {
        border: 1px solid black;
        margin: 0px auto;
      }
      body {
        text-align: center;
      }
    </style>
  </head>
  <body>
    <h1>User Data</h1>
    <table>
      <tr>
        <th>Id</th>
        <th>Username</th>
        <th>Email</th>
      </tr>
      <% for(user of result) { %>
      <tr>
        <td><%=user.id%></td>
        <td>
          <%=user.username%>
          <form method="get" action="/users/<%=user.id%>/edit">
            <button>Edit username</button>
          </form>
        </td>
        <td><%=user.email%></td>
        <td>
          <form method="get" action="/users/<%=user.id%>/delete">
            <button>Delete User</button>
          </form>
        </td>
      </tr>
      <%}%>
    </table>
    <form method="get" action="users/new"><button>Add New User</button></form>
  </body>
</html>
